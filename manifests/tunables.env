# ==============================================================================
# Agent Substrate Micro-VM Baseline Invariants (Fixed Rig Settings)
# ==============================================================================
# Fixed harness baseline settings for glutton_mem_1gi_microvm.
# All optimization hypotheses target the Go codebase (cmd/ateom-microvm, cmd/atelet)
# via [ACTION: CODE_REFACTOR] mutations.
# ==============================================================================

export BENCHMARK_SANDBOX_CLASS="microvm"
export BENCHMARK_ACTOR_MEMORY="1536Mi"
export BENCHMARK_MEM_TARGET="1Gi"
export BENCHMARK_MEM_CHURN="64Mi"
export BENCHMARK_USERS=1
export BENCHMARK_DURATION="2m"

# ==============================================================================
# Feature Flags (To be proposed and declared by APO engine)
# ==============================================================================
# Variables prefixed with FEATURE_* will be automatically forwarded to ateom-microvm
# and atelet containers via ConfigMap / DaemonSet environment injection.

